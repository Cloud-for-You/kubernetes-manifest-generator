render_bootstrap() {

  local name="${1}"
  local appconfig="${2}"
  if [ -z "${appconfig}" ]; then
    echo "Render default config for ${name}"
    echo "###################################"
  else
    echo "Render custom config for ${name}"
    echo "${appconfig}" > ${ROOTDIR}/resources/bootstrap/${name}.yaml
  fi

}
